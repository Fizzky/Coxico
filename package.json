{
  "name": "coxico-monorepo",
  "version": "1.0.0",
  "description": "Coxico manga reader platform",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
